<%= form_for(current_user.active_relationships.build,remote: true) do |f|%> <%# 「remote: true」でAjaxを使える%>
    <div><%= hidden_field_tag :followed_id, @user.id %></div>
    <%= f.submit "Follow",class:"btn btn-primary" %>
<%end%>